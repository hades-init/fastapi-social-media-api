# ── Core API Framework ────────────────────────────────────────────────────────
# Covers: fastapi-cli, uvicorn[standard], starlette, email-validator,
#         python-multipart, httpx, typer, rich, uvloop, watchfiles, websockets
fastapi[standard]>=0.135.1

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.49
alembic>=1.18.4
psycopg[binary]>=3.3.3          # covers psycopg + psycopg-binary

# ── Settings & Validation ─────────────────────────────────────────────────────
pydantic-settings>=2.13.1       # covers pydantic + python-dotenv
pydantic-extra-types>=2.11.1

# ── Authentication ────────────────────────────────────────────────────────────
pyjwt>=2.12.1
pwdlib[argon2]>=0.3.0           # covers pwdlib + argon2-cffi

# ── Templating ────────────────────────────────────────────────────────────────
jinja2>=3.1.6